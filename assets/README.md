# assets/ 目录

存放需要共享归档的**非文档类资料文件**（图片、表格、数据集等小文件）。

```
assets/
├── images/     # 图片素材
├── data/       # 数据文件（CSV 等）
└── ...
```

> 单个文件超过 10MB：请改用 GitHub Releases 发布，不要在仓库里直接提交。
> 内容提交走 Pull Request 审核流程。
