# 协作与审阅规范（CONTRIBUTING）

本文档定义团队在本仓库中的协作规则，**所有成员在提交任何内容前请先阅读**。

---

## 1. 角色分工

| 角色 | 说明 |
| --- | --- |
| 仓库管理员（Owner） | 拥有最高权限，管理成员、分支保护、Release 发布 |
| 维护者（Maintainer） | 有合并权限，负责审核他人提交的内容 |
| 成员（Member） | 可推送自己的分支、发起 Pull Request、参与 Review |

> 默认：所有人（包括管理员）提交内容都走 Pull Request 审核，避免绕过检查直接改 `main`。

## 2. 分支策略

- `main`：正式分支。**受保护，任何人不能直接 push**，只能通过审核通过的 Pull Request 合并。
- 功能/文档分支：命名格式 `docs/<名字>-<内容概述>` 或 `feat/<名字>-<内容概述>`。
  - 示例：`docs/xiaoming-新增周报模板`、`feat/lili-爬虫脚本v1`
- 分支完成后删除（合并时勾选 Delete branch），保持仓库干净。

## 3. 发布文件的标准流程

**Step 1 — 建分支**（永远不要直接在 main 上改）：

```bash
git checkout -b docs/xiaoming-发布周报
```

**Step 2 — 加文件**：按 README 的目录约定放入 `docs/` 或 `assets/` 下对应子目录。文件命名清晰、无乱码。

**Step 3 — 提交推送**：

```bash
git add .
git commit -m "docs: 发布第 3 周项目周报"
git push -u origin docs/xiaoming-发布周报
```

**Step 4 — 发起 Pull Request**：

- 标题写清"做了什么"：`docs: 发布第 3 周项目周报`
- 描述用模板填写：背景、改了什么、影响范围、是否待确认
- 指派一位维护者作为 Reviewer

**Step 5 — 等待审核与合并**：

- 审核通过（Approve）→ 合并进 `main`
- 被打回（Request changes）→ 按留言修改 → 重新 push，同一 PR 会自动更新，无需重开

## 4. 检查（Review）他人文件的标准流程

作为 Reviewer，检查一个 PR 时按以下清单过一遍：

- [ ] **内容相关**：与 PR 描述一致，没有夹带无关改动
- [ ] **文件规范**：位置正确（docs/assets）、命名清晰、格式正常（编码不乱、可打开）
- [ ] **质量**：无错别字/明显错误，引用的数据或链接有效
- [ ] **安全与合规**：不含密钥、密码、内部敏感信息
- [ ] **约定**：符合仓库既有分类与命名习惯

结论处理：

- 全部通过 → 点击 **Approve**，随后可合并
- 有小问题 → 在具体行上留言（可批准合并，让作者自行跟进修）
- 有重大问题 → **Request changes** 打回，写明修改要求

## 5. 大型 / 二进制文件的发布

单个文件超过 10MB（如数据集、安装包、成品图片）不建议直接放 Git 仓库，改用 **GitHub Releases**：

1. 先合并与文件相关的代码/文档 PR；
2. 由管理员在仓库页 `Releases → Draft a new release` 上传文件；
3. 在 README 的"下载"区更新链接。

## 6. 提交信息规范（Commit Message）

简短说明意图即可，建议带前缀：

| 前缀 | 场景 | 示例 |
| --- | --- | --- |
| `docs:` | 文档/资料 | `docs: 更新项目进度` |
| `feat:` | 新功能/新内容 | `feat: 新增数据清洗脚本` |
| `fix:` | 修复问题 | `fix: 修正周报里的日期错误` |
| `chore:` | 杂项/维护 | `chore: 整理 docs 目录` |

## 7. 冲突处理

合并时提示冲突（两个人改了同一处）时：

1. 在本地 `git pull` 同步 main；
2. 切回自己的分支：`git merge main`；
3. 编辑器会标出冲突区域 `<<<<<<< / ======= / >>>>>>>`，手动保留正确内容；
4. 保存后 `git add . && git commit`，再 push 即可。

## 8. 常见问题（FAQ）

**Q: 误把内容直接 push 到 main 了？**
A: `main` 受保护时会被拒绝。若已成功 push，请立即告知管理员用 `git revert` 回滚，不要自行强推。

**Q: 想撤回已经合并的内容？**
A: 用 `git revert <commit>` 生成反向提交，再走 PR 合并。

**Q: 不知道文件放哪个目录？**
A: 放到最贴近主题的 `docs/` 子目录；实在不明确就放 `docs/待整理/`，Review 时大家会帮你归类。
